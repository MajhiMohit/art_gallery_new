# ── Development (local) ──────────────────────────────────────────────────────
# Vite proxy in vite.config.js forwards /api/* → http://localhost:8080
# This keeps dev calls going through the local Spring Boot server
VITE_API_URL=/api
